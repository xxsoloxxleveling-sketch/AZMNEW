/**
 * Single switch for restoring the public registration forms in the future.
 * The form components remain in the codebase; change this to `true` to expose them again.
 */
export const PUBLIC_REGISTRATION_OPEN = false;
