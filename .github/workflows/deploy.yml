name: Build & Auto-Deploy to Hostinger Branch

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'

    - name: Install Dependencies
      run: npm ci

    - name: Build Static Production Assets
      run: npm run build

    - name: Deploy Compiled Files to 'deploy' Branch (For Hostinger Static Git)
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: deploy
        folder: dist
        clean: true
